import { aboutHero } from './hero';
import { scale } from './scale';
import { slider } from './slider';
import { values } from './values';

export const about = () => {
  aboutHero();
  scale();
  slider();
  values();
};
